import type { ThemeComponents } from '@chakra-ui/react'

const FormError: ThemeComponents['FormError'] = {
  variants: {
    jobs: {
      text: {
        color: 'red.400',
      },
    },
  },
}

export default FormError
