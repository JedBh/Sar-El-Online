import type { ThemeComponents } from '@chakra-ui/react'

const Container: ThemeComponents['Container'] = {
  baseStyle: {
    paddingLeft: [4, 8, 10, 0, 0],
    paddingRight: [4, 8, 10, 0, 0],
    maxW: 'container.xl',
  },
}

export default Container
